# DeepSeek Copilot 工作台

DeepSeek Copilot 工作台是一个本地 Web 控制台，用于组合 DeepSeek 对话、受控的 Copilot/Codex 风格 CLI 运行器、轻量级 RAG、审计日志以及基于 Docker 的部署能力。

## 功能特性

- 基于 FastAPI 的后端，提供对话、模型配置、Agent、知识库和日志 API。
- 基于 React 的前端，包含仪表盘、Agent 运行、知识检索、日志和设置视图。
- 支持本地导入 `.txt`、`.md`、`.pdf` 和 `.docx` 文档。
- 使用哈希词袋嵌入实现简单的本地向量式检索。
- 为 Agent 执行提供权限检查，并写入只追加的 JSONL 审计日志。
- 提供 Docker Compose 服务，包含后端、前端以及可选的 Chroma。

## 快速开始

```powershell
copy .env.example .env
notepad .env
.\start-copilot-deepseek.cmd
```

打开 `http://localhost:3000`。

## Docker

```powershell
copy .env.example .env
docker compose up --build
```

## 重要环境变量

- `DEEPSEEK_API_KEY`：DeepSeek API 密钥。
- `DEEPSEEK_BASE_URL`：默认为 `https://api.deepseek.com`。
- `DEFAULT_MODEL`：默认为 `deepseek-chat`。
- `WORKSPACE_ROOT`：暴露给 Agent 运行器的目录。
- `COPILOT_COMMAND`：Agent 运行器使用的 CLI 命令，例如 `codex`。
- `AGENT_PERMISSION`：可选值为 `READ_ONLY`、`EDIT_ALLOWED`、`COMMAND_ALLOWED` 或 `ADMIN`。

## API 接口

- `POST /api/chat`
- `POST /api/models/test`
- `GET /api/models/config`
- `POST /api/copilot/runs`
- `GET /api/copilot/runs`
- `GET /api/copilot/runs/{id}`
- `GET /api/copilot/runs/{id}/events`
- `DELETE /api/copilot/runs/{id}`
- `POST /api/copilot/runs/{id}/stop`
- `POST /api/copilot/runs/{id}/permissions`
- `POST /api/agent/run`
- `GET /api/agent/runs/{id}`
- `GET /api/agent/runs/{id}/logs`
- `POST /api/agent/runs/{id}/stop`
- `POST /api/kb/upload`
- `POST /api/kb/ingest`
- `GET /api/kb/files`
- `DELETE /api/kb/files/{id}`
- `POST /api/kb/query`
- `GET /api/logs`

## 安全模型

Agent 命令会在执行前进行检查。除非 `AGENT_PERMISSION=ADMIN`，否则会阻止递归删除、磁盘格式化、强制 Git 重置等破坏性 shell 模式。每一次命令请求、执行结果、模型测试、上传、导入和查询都会写入 `logs/audit.jsonl`。
