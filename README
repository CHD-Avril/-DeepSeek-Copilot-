# DeepSeek Copilot 工作台

DeepSeek Copilot 工作台是一个本地 Web 控制台，用于把 DeepSeek 对话、受控 Agent 执行、轻量级知识库检索、审计日志和 Docker 部署整合到同一个界面中。

## 当前能力

- FastAPI 后端：提供聊天、模型配置、Copilot Agent、传统命令 Agent、知识库和审计日志 API。
- React 前端：提供总览、AI Agent、知识库、审计日志和配置视图。
- Copilot Agent：使用 DeepSeek 返回结构化 JSON，按步骤调用受控工具，并在前端展示完整事件流。
- 持久化运行记录：Copilot runs/events 写入 SQLite，默认路径为 `logs/copilot.sqlite3`。
- 权限确认：命令执行、文件写入、补丁应用、文件创建和删除都需要前端确认。
- 文件工具：支持 `preview_patch`、`apply_patch`、`write_file`、`create_file`、`delete_file`。
- 知识库：支持导入 `.txt`、`.md`、`.pdf`、`.docx`，并用本地哈希词袋向量做轻量检索。
- 审计日志：模型测试、命令请求、执行结果、上传、导入和查询会写入 `logs/audit.jsonl`。

## 快速开始

```powershell
copy .env.example .env
notepad .env
.\start-copilot-deepseek.cmd
```

启动后打开：

- 前端：`http://localhost:3000`
- 后端健康检查：`http://localhost:8000/health`

也可以手动启动：

```powershell
cd backend
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

```powershell
cd frontend
npm.cmd run dev
```

## Docker

```powershell
copy .env.example .env
docker compose up --build
```

## 重要环境变量

- `DEEPSEEK_API_KEY`：DeepSeek API 密钥。
- `DEEPSEEK_BASE_URL`：DeepSeek API 地址，默认 `https://api.deepseek.com`。
- `DEFAULT_MODEL`：默认模型，默认 `deepseek-chat`。
- `WORKSPACE_ROOT`：暴露给 Agent 的工作目录。
- `KNOWLEDGE_BASE_DIR`：知识库文件目录。
- `LOG_DIR`：日志和 Copilot SQLite 数据库目录。
- `AGENT_PERMISSION`：传统命令 Agent 的权限模式，可选 `READ_ONLY`、`EDIT_ALLOWED`、`COMMAND_ALLOWED`、`ADMIN`。
- `BACKEND_CORS_ORIGINS`：允许访问后端的前端来源列表。

## Copilot Agent 流程

1. 前端调用 `POST /api/copilot/runs` 创建任务。
2. 后端把 run 状态和 events 写入 SQLite。
3. DeepSeek 必须返回一个 JSON 对象：`tool_call` 或 `final`。
4. 后端校验 JSON schema；如果格式错误，会记录 `model_json_invalid` 并尝试修复一次。
5. 工具调用、工具结果、权限请求、权限决定、最终答案都会进入事件流。
6. 前端轮询 run 和 events，显示历史 run、当前状态、最终结果和权限确认弹窗。

## Copilot 工具

- `list_dir(path)`：列出允许目录下的文件。
- `read_file(path)`：读取 UTF-8 文本文件。
- `search_text(query, path)`：搜索文本。
- `get_project_tree()`：返回简化项目树。
- `run_command(command)`：执行允许的命令，需要确认。
- `preview_patch(patch)`：校验并预览 unified diff，不写文件。
- `apply_patch(patch)`：应用 unified diff，需要确认，并先执行 `git apply --check`。
- `write_file(path, content)`：覆盖已有文件，需要确认。
- `create_file(path, content)`：创建新文件，需要确认。
- `delete_file(path)`：删除文件，需要确认。

## API 接口

- `POST /api/chat`
- `POST /api/models/test`
- `GET /api/models/config`
- `POST /api/copilot/runs`
- `GET /api/copilot/runs`
- `GET /api/copilot/runs/{id}`
- `GET /api/copilot/runs/{id}/events`
- `DELETE /api/copilot/runs/{id}`
- `POST /api/copilot/runs/{id}/stop`
- `POST /api/copilot/runs/{id}/permissions`
- `POST /api/agent/run`
- `GET /api/agent/runs/{id}`
- `GET /api/agent/runs/{id}/logs`
- `POST /api/agent/runs/{id}/stop`
- `POST /api/kb/upload`
- `POST /api/kb/ingest`
- `GET /api/kb/files`
- `DELETE /api/kb/files/{id}`
- `POST /api/kb/query`
- `GET /api/logs`

## 安全模型

Copilot 只允许访问项目根目录、`workspace` 和 `knowledge_base` 等允许路径，并阻止 `.env`、私钥和密钥文件。文件修改类工具必须通过前端权限确认，补丁应用前会先做 `git apply --check`。命令执行会经过权限策略检查，除非 `AGENT_PERMISSION=ADMIN`，否则会阻止递归删除、磁盘格式化、强制 Git 重置等危险命令模式。

## 开发验证

```powershell
python -m compileall backend\app
```

```powershell
cd backend
.\.venv\Scripts\python.exe -c "from app.main import app; print(app.title)"
```

```powershell
cd frontend
npm.cmd run build
```
